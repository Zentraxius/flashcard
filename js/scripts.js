$(document).ready(function () {
  $("button#JavaScript").click(function () {
    $("body").removeClass();
    alert(
      "An object-oriented computer programming language commonly used to create interactive effects within web browsers."
    );
  });

  $("button#Operators").click(function () {
    $("body").removeClass();
    alert(
      "+, -, *, /, and % are called operators. An operator is a special character (or characters) that indicates an action to be performed."
    );
  });

  $("button#Variables").click(function () {
    $("body").removeClass();
    alert(
      "Variables can be thought of as containers used to store information. They allow for a way to label data with a descriptive name."
    );
  });

  $("button#Variable naming conventions").click(function () {
    $("body").removeClass();
    alert("");
  });

  $("button#Functions").click(function () {
    $("body").removeClass();
    alert(
      "A function is a block of code that performs an action and returns a result; optionally takes arguments"
    );
  });

  $("button#Methods").click(function () {
    $("body").removeClass();
    alert(
      "A method is an action run on a piece of data; you can think of it as a message you send to a piece of data, and the result is the response."
    );
  });

  $("button#Arguments").click(function () {
    $("body").removeClass();
    alert(
      "Some methods take one or more arguments that provide the method with additional information to help it perform its action."
    );
  });

  $("button#Parameters").click(function () {
    $("body").removeClass();
    alert("Is a variable that's assigned to the argument.");
  });

  $("button#Return").click(function () {
    $("body").removeClass();
    alert(
      "Once called, many functions will provide a result of some kind. This is known as returning, and the result is often referred to as a return value."
    );
  });

  $("button#Chaining methods").click(function () {
    $("body").removeClass();
    alert("Calling a method directly on the return value of another method.");
  });

  $("button#Strings").click(function () {
    $("body").removeClass();
    alert(
      'A string is a set of characters enclosed in quotations. "hello" is an example'
    );
  });

  $("button#Booleans").click(function () {
    $("body").removeClass();
    alert("A boolean is either true or false.");
  });

  $("button#Undefined").click(function () {
    $("body").removeClass();
    alert("This means something hasn't been defined in our code yet.");
  });

  $("button#NaN").click(function () {
    $("body").removeClass();
    alert("Not a Number");
  });

  $("button#Null").click(function () {
    $("body").removeClass();
    alert("Null is nothing.");
  });

  $("button#Alert").click(function () {
    $("body").removeClass();
    alert("Displays a window. Opens a dialog box and returns undefined.");
  });

  $("button#Comments").click(function () {
    $("body").removeClass();
    alert(
      "Anything following // on a line in JavaScript is ignored by the computer. Comments are a convenient way to leave notes in your code for yourself or other programmers."
    );
  });

  $("button#jQuery").click(function () {
    $("body").removeClass();
    alert("Is a Javascript library");
  });

  $("button#Attributes").click(function () {
    $("body").removeClass();
    alert(
      "Specifies one or more classnames for an element. The class attribute is mostly used to point to a class in a style sheet. However, it can also be used by a JavaScript (via the HTML DOM) to make changes to HTML elements with a specified class."
    );
  });
});

{
  /* <button class="btn btn-danger" id="red">
  Red
</button>;
$(document).ready(function () {
  $("button#green").click(function () {
    $("body").removeClass("yellow-background", "red-background");
    $("body").addClass("green-background");
  });

  $("button#yellow").click(function () {
    $("body").removeClass();
    $("body").addClass("yellow-background");
  });

  $("button#red").click(function () {
    $("body").removeClass();
    $("body").addClass("red-background");
  });
}); */
}

// <div id="walrus-hidden">
// <p>Let's play peek-a-boo. <span class="clickable">Click here</span> to see the surprise!</p>
// </div>

// $(document).ready(function () {
//  $(".clickable").click(function () {
//    $("#walrus-showing").slideToggle();
//   $("#walrus-hidden").slideToggle();
//  });
// });
